# Targets

Built-in target:

- `stddef`: emits deterministic C header output.

Main options:

- `array_name` (default `nuperf_table`)
- `emit_header_guard` (default `true`)
- `guard_prefix` (default `NUPERF_TABLE`)
