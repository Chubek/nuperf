# Usage

## CLI

```bash
nuperf version
nuperf list-methods
nuperf list-targets
nuperf build -i keys.txt -o table.h -m stdmeth -t stddef --option array_name=my_table
```

Input format: one key per line.
